# =====================================================================
#  Smart Traffic Command Center — Python dependencies
#  Core stack is enough to run the FULL platform; optional libs unlock
#  extra models and are auto-detected at runtime (graceful fallbacks).
# =====================================================================

# ── Core data / ML ────────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
statsmodels>=0.14
joblib>=1.3
pyyaml>=6.0
pyarrow>=14.0          # parquet engine

# ── Dashboard ─────────────────────────────────────────────────────
streamlit>=1.30
plotly>=5.18

# ── REST API ──────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# ── Dev / test ────────────────────────────────────────────────────
pytest>=7.4
httpx>=0.26

# ── Optional (install for maximum capability) ─────────────────────
# pip install xgboost lightgbm prophet mlflow shap folium streamlit-folium
#   xgboost / lightgbm  -> gradient-boosting model candidates
#   prophet             -> additive forecasting model
#   mlflow              -> experiment tracking + model registry backend
#   shap                -> model explainability
#   folium              -> alternative Leaflet maps in the dashboard
#   tensorflow          -> LSTM forecasting (heavy; Python < 3.12)
