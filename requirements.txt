# ── Core (required) ───────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.10
statsmodels>=0.14
joblib>=1.3
pyyaml>=6.0

# ── Dashboard ─────────────────────────────────────────────────────
streamlit>=1.30
plotly>=5.18

# ── Optional (auto-detected; code degrades gracefully if absent) ──
# Install for full capability:
xgboost>=2.0           ; python_version >= "3.8"
lightgbm>=4.0          ; python_version >= "3.8"
prophet>=1.1           ; python_version >= "3.8"
mlflow>=2.9            ; python_version >= "3.8"
shap>=0.44             ; python_version >= "3.8"
folium>=0.15           ; python_version >= "3.8"
streamlit-folium>=0.18 ; python_version >= "3.8"
# tensorflow>=2.13     ; python_version <  "3.12"   # LSTM (heavy; optional)
